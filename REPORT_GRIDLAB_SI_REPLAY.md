# GRIDLAB — Replay-as-Live на архивах фьючерсов MOEX: отчёт

Дата: 2026-06-16. Экран **`/replay`** (отдельный от Live-терминала и Лаборатории, светлая тема).
Тот же единый Quoter и адаптивная сетка, что в Live и бэктесте.

## 1. Данные (архивы в `D:\DEFI`)
Формат у всех — Finam-CSV `<TICKER>,<PER>,<DATE>,<TIME>,<OPEN>,<HIGH>,<LOW>,<CLOSE>,<VOL>`,
`PER=1` → **1-минутные** бары, OHLCV есть. Тот же загрузчик, что у ETHBTC (`localdata.py`).

| Инстр. | Что | Период | Баров (1m) | Размер | Последняя цена |
|--------|-----|--------|-----------|--------|----------------|
| **Si** | USD/RUB фьючерс | 2009-01-11 → 2026-05-29 | 3 506 321 | 168 МБ | 71 545 |
| **Eu** | EUR/RUB фьючерс | 2009-06-16 → 2026-05-29 | 2 379 040 | 112 МБ | 83 301 |
| **ED** | EUR/USD фьючерс | 2009-06-15 → 2026-05-29 | 2 939 685 | 148 МБ | 1.1645 |
| **RTS** | фьючерс индекса РТС | 2009-01-11 → 2026-05-31 | 3 562 641 | 184 МБ | 113 820 |

Загрузка сырых 1m баров — один раз на инструмент (Si ≈ 3.5 млн строк, ~10–14 с), потом
ресэмпл в выбранный ТФ кэшируется. Архивы оставлены на месте (в `D:\DEFI`), доступ — по
спецификации инструмента (`app/engine/futures.py`). Выпадающий список в UI: **Si (по умолчанию) / Eu / ED / RTS**.

## 2. Это ЖИВАЯ эмуляция, а не батч
Цены текут через ТОТ ЖЕ тиковый конвейер, что и Live: `PaperEngine.on_tick` + единый
`Quoter`. Внутри каждого бара движку подаётся последовательность тиков
`open → ближний экстремум → дальний экстремум → close` (порядок H/L внутри бара честно
неизвестен — идём от open к ближнему). На каждом тике:
- проверяются и исполняются пересечённые лимитки сетки (метка сделки появляется в тот момент);
- пересобирается сетка через Quoter (инвентарный скос, адаптивный шаг);
- **эквити и баланс сэмплируются ПОТИКОВО** — кривая растёт/падает на глазах, не появляется в конце;
- журнал событий пополняется построчно.

Сервер не считает прогон целиком: бар проигрывается, кадр шлётся по WebSocket, затем
пауза по таймеру скорости — эмулируется течение времени. Проверено WebSocket-клиентом:
за серию кадров `idx` растёт (0→22), `equity` меняется потиково (100 000 → 100 083.8 ₽);
в браузере — бегущий график, кривая эквити, 44 строки журнала, 2 активных + 2 превью-линии сетки.

**Регулятор времени:** ползунок + пресеты скорости (**бар/сек**, 0.5…120), **пауза**,
**шаг на 1 бар** (на паузе), **перемотка** (ползунок прогресса, мгновенный пересчёт до точки),
**рестарт**. Замедлил — видно каждую сделку; ускорил — день за секунды.

## 3. Модель исполнения (честно, на ценах без стакана)
- **строго 1 контракт на ордер**, атомарно: цена коснулась уровня лимитки → 1 контракт
  исполнен целиком (никаких частичных, никаких долларовых размеров);
- **очередь в стакане НЕ эмулируется** — в архиве её нет, только цены; для 1 контракта это
  наименьшее искажение, честное допущение на потоке цен;
- **мейкер-комиссия = 0** (тариф ALOR «Срочный Стандарт»), **funding нет** (фьючерс);
- **PnL в рублях** через стоимость пункта (`point_value`), маржа/ГО для контроля позиции;
- масштаб — НЕ через размер ордера (1 контракт фикс), а через ПОРТФЕЛЬ: тот же робот на
  N фьючерсах (архитектурно заложено, Si первым).

### Стоимость пункта (₽ за 1 пункт за 1 контракт)
| Инстр. | point_value | Шаг цены | Точность |
|--------|-------------|----------|----------|
| Si | **1.0 ₽** | 1 | точно (спека MOEX) |
| Eu | **1.0 ₽** | 1 | точно (спека MOEX) |
| ED | ~9.0 ₽ | 0.0001 | **приближённо** (зависит от USD/RUB; помечено ≈) |
| RTS | ~1.8 ₽ | 10 | **приближённо** (зависит от USD/RUB; помечено ≈) |

ED/RTS считаются по ориентировочному курсу USD/RUB≈90, которого в архиве цен нет — в UI
такие инструменты помечены «≈прибл.», стоимость уточняется позже (ALOR/спека).

## 4. Источник за интерфейсом (ALOR — позже)
Источник цен изолирован: сейчас локальный архив (`spec_path` → файл, `localdata.raw_series`).
ALOR API подключается вторым источником за тем же интерфейсом, без переписывания движка/фронта
(тогда же появится живой стакан и мейкер-исполнение по очереди — см. §3).

## 5. Первые числа прогона Si (1 контракт, счёт 100 000 ₽, окно 1800 баров)
Числа без интерпретаций — мейкер 0, funding 0, PnL в рублях.

| Инстр. | ТФ | k (gs) | Режим | Сделок | Реализ., ₽ | Эквити, ₽ | ROI |
|--------|----|----|-------|--------|-----------|-----------|-----|
| Si | 5m | 1.8 | эвристика | 102 | −3 395 | 96 636 | −3.36% |
| Si | 5m | 1.8 | A-S | 227 | −5 768 | 94 232 | −5.77% |
| Si | 15m | 1.8 | эвристика | 80 | −1 081 | 98 907 | −1.09% |
| Si | 5m | 2.5 | эвристика | 51 | +510 | 100 521 | +0.52% |

Зависимость: шире шаг (k↑) и крупнее ТФ → меньше сделок. Решения по числам — за владельцем.

## 6. Файлы
- `app/engine/futures.py` — спеки Si/Eu/ED/RTS (point_value, шаг, ГО, файл, approx-флаг).
- `app/engine/replay.py` — `ReplaySession`: часы проигрывания, регулятор скорости/пауза/перемотка/рестарт, кадры в ₽.
- `app/engine/paper.py` — фьючерсный режим: `mult` (₽/пункт), денежные расчёты в рублях.
- `app/strategy/quoting.py`, `models.py` — `contract_qty` (размер ордера в контрактах).
- `app/marketdata/localdata.py` — мультиинструментная загрузка + raw 1m серии для Replay.
- `web/replay.html` + `web/replay.js` — терминал `/replay` (дропдаун, контролы, ₽-ярлыки).
- `app/main.py` — роуты `/replay`, `/api/replay/instruments`, `/api/replay/data_info`, ws `/ws/replay`.

## 7. Как открыть
Терминал Live → ссылка в шапке, либо прямо `http://localhost:8100/replay`. Выбрать инструмент
(Si по умолчанию), ТФ, скорость; ▶ играть, ❚❚ пауза, ▷| шаг, ↺ рестарт, ползунок — перемотка.
Параметры стратегии справа; «Применить» перематывает прогон с текущего бара под новые параметры.
