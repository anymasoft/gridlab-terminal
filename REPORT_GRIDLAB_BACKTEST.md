# GRIDLAB — Backtest Lab на ETHBTC: отчёт

Дата: 2026-06-15. Бэктест использует ТОТ ЖЕ единый Quoter, что и Live (см. `REPORT_QUOTER_UNIFY.md`) —
проверяется ровно то, что торгуется.

## 1. Данные (`D:\DEFI\ETHBTC.txt`)
- Формат: Finam-CSV `<TICKER>,<PER>,<DATE>,<TIME>,<OPEN>,<HIGH>,<LOW>,<CLOSE>,<VOL>`, OHLCV есть.
- ТФ базовый: **1 минута** (`PER=1`). Период всего файла: **2017-07-14 → 2026-05-31**, 4 599 547 свечей, 313 МБ.
- Загрузчик (`app/marketdata/localdata.py`) грузит с **2024-01-01** (ранние 2017 — плоские 0.08, VOL=0):
  **1 217 703** баров 1m, ресэмпл в 5m/15m/30m/1h/4h. Загрузка ~6с (один раз), ресэмпл <1с.
- Цена ETHBTC ≈ **0.027–0.035** в 2024–2026 — **узкий боковик** (родная среда грида), месячный размах 5–10%.
- **ETHBTC — спот, не перп → funding неприменим.** В бэктесте funding принудительно выключен
  (`cost.default_funding_rate=0`, `funding={}`). Это честное отличие от Live на перпах Bybit.

## 2. Что построено (4 режима, единый Quoter)
`app/backtest/lab.py` + API `/api/lab/*` + экран **`/lab`** (Optimization Lab, светлая тема):
1. **Single** — один прогон: ROI, просадка, сделки, комиссии, funding, profit factor, Sharpe, equity curve (SVG).
2. **Sweep** — полный перебор сетки параметров; ранжирование по **robust score = ROI − 0.5·DD − 0.2·fees%**
   (ликвидация = −100, дисквалификация). Плюс **доля прибыльных комбинаций** (ширина зоны).
3. **Heatmap** — 2D-карта (X=Grid ATR, Y=SL ATR и др.), цвет = метрика; подсвечивает лучшую точку и
   считает **долю прибыльных соседей** вокруг оптимума (изолированный пик = оверфит).
4. **Walk-Forward** — оптимизация на train-окне (30д) → проверка на следующем test-окне (7д) → сдвиг;
   in-sample vs out-of-sample по окнам + сводка (средний OOS ROI, доля прибыльных OOS-окон).
Экспорт JSON/CSV. Запуск тяжёлых прогонов в threadpool (`asyncio.to_thread`) — live-сессия не блокируется.

## 3. Метрика против оверфита
Не голый profit, а robust score; и главное — **ширина прибыльной зоны**, а не одна точка. На heatmap
видно: сплошная зелёная область = робастно; одинокий зелёный пик среди красного = везение/оверфит.

## 4. Результаты на ETHBTC (15m, ~180 дней, 17 000 свечей)
**Heatmap ROI (Grid ATR × SL ATR), % за период:**
```
        gs=1.0  1.25   1.5   1.75  2.0   2.5   3.0
sl=2:   -3.88  -3.22 -1.94 -1.56 -1.77 -0.92 -0.74
sl=3:   -3.26  -2.96 -2.44 -1.64 -1.21 -0.92 -0.91
sl=4:   -2.34  -2.13 -1.75 -1.39 -1.96 -1.10 -0.94
sl=5:   -2.42  -2.02 -1.46 -1.35 -1.36 -0.81 -1.56
sl=6:   -2.15  -1.73 -1.62 -1.56 -1.69 -0.74 -0.99
```
- **Прибыльная доля = 0%** (0 из 35 комбинаций в плюсе). Лучшая (наименее убыточная) точка
  `gs=2.5, sl=6` = −0.74%; прибыльных соседей 0/6.
- Закономерность чёткая: **чем шире шаг (gs↑) — тем меньше убыток** (меньше сделок → меньше комиссий).
  Это не альфа, а минимизация издержек: 796–1033 сделки за 180 дней, maker-комиссии доминируют.
- Режимы: heuristic (−0.585%) менее убыточен, чем avellaneda (−1.462%) на gs=1.5/sl=5.

**Walk-forward (12 окон, train 30д / test 7д):**
- Средний **OOS ROI ≈ −0.009%** (статистический ноль), прибыльных OOS-окон **6/12 (50%)** — подбрасывание монеты.
- Лучший `grid_spacing` по окнам скачет: `[3.0, 1.0, 3.0, 3.0, 3.0, 2.5, 2.5, 2.5, 2.5, 2.5, 2.5, 2.5]` —
  параметр не стабилизируется → **устойчивого edge нет**.

**На 1h (меньше сделок):** heuristic gs=2.5/sl=5 → ROI −0.13%, 52 сделки, fees $0.50 — ближе к нулю,
потому что широкая сетка на крупном ТФ почти не торгует.

## 5. Сводка чисел (без интерпретаций — выводы за владельцем)
- 30 дней (15m): лучшая комбинация ≈ +0.4% ROI.
- 180 дней (15m): диапазон ROI по всем 35 комбинациям от −0.74% до −3.88%; прибыльных 0/35.
- Walk-forward (12 окон, train 30д/test 7д): средний OOS ROI = −0.009%; прибыльных окон 6/12;
  лучший `grid_spacing` по окнам: 3.0,1.0,3.0,3.0,3.0,2.5,2.5,2.5,2.5,2.5,2.5,2.5.
- Зависимость: больше шаг (gs↑) и крупнее ТФ → меньше сделок (52 на 1h против 796–1033 на 15m/180д)
  → меньше суммарная комиссия.
- Комиссии: maker 0.01%, taker 0.06% (Bybit деривативы). Funding=0 (ETHBTC спот).
- **Замечание по модели:** параметр **TP в текущем Quoter не влияет** (round-trip реализуется
  перекотировкой пары, а не отдельным TP-ордером) — в sweep его включать смысла нет (помечено в UI).

Инструмент даёт цифры (single/sweep/heatmap/walk-forward) и меру ширины прибыльной зоны — решения
по ним принимает владелец.

## 6. Как пользоваться
Терминал → ссылка **«Лаборатория»** (или прямо `/lab`). Слева — источник (ETHBTC локально / Bybit),
ТФ, число свечей, база стратегии, веса метрики. Вкладки Single / Sweep / Heatmap / Walk-Forward.
«Найти робастные параметры» / «Построить heatmap» / «Запустить walk-forward». Экспорт JSON/CSV.
